# BET — Business Experience Together\n\n**Business Experience Intelligence** — **See what happened before you decide.**\n\nThis is the current canonical **production-preparation package**. It contains the final product/architecture MASTER, the existing visual prototype, and a portable backend/database/security skeleton intended to become the real closed-beta system rather than being thrown away.\n\n## What is already prepared\n- canonical `ARCHITECTURE.md`;\n- `DATABASE-MAP.md` and PostgreSQL migrations in `db/migrations`;\n- TypeScript modular-monolith API in `apps/api`;\n- managed-auth adapter boundary;\n- Member sync, businesses, Business Snapshots, separate Experiences, Ask queries, notifications, Private Notes, MatchPool-gated conversations, technical Support + human escalation, admin list/verification queue;\n- durable PostgreSQL job queue;\n- provider boundaries for AI, auth, storage, transcription and email;\n- security baseline, RLS defense-in-depth, backup/restore scripts, CI, Docker development config, staging/production runbook;\n- payment-safety messaging remains disabled by feature flag;\n- existing HTML pages remain product/UI reference until wired to authenticated production state.\n\n## What cannot be completed without owner authorization\nExternal accounts/credentials: managed Postgres/Auth/private Storage, domain/DNS/WAF, AI provider, transactional email, and later billing. See `OWNER-ACTIONS.md`.\n\n## Important\nDo not send the current static HTML URL to beta users as if BET were live. The next step is provider authorization + staging deployment + E2E/security tests. The architecture and code are intentionally structured so this connection stage does not require a rewrite.\n\n## Core commands once dependencies are installed\n`npm run check`\n\nLocal PostgreSQL: `infra/docker/docker-compose.yml`.\nEnvironment template: `.env.example`.\nDeployment instructions: `docs/DEPLOYMENT-RUNBOOK.md`.\n